package org.momu.tOCplugin.listener;

import org.bukkit.event.Listener;
import org.momu.tOCplugin.TOCpluginNative;

public class PlayerMoveListener
implements Listener {
    public PlayerMoveListener(TOCpluginNative plugin) {
    }
}